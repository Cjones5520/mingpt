# Core dependencies
torch>=1.7.0

# PDF and image processing dependencies for Credible signature scanner
pdf2image>=3.1.0
pytesseract>=0.3.10
opencv-python>=4.5.0
pandas>=1.3.0
Pillow>=8.0.0
openpyxl>=3.0.0

# Optional dependencies for testing (if transformers tests are needed)
transformers>=4.0.0